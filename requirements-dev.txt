# Docker-side (bpy-FREE) dev/test dependencies for rules/, texturegen/, postfx/, labeltools/.
# Blender ships its own bundled numpy at runtime; this is only for testing the pure-Python
# modules in the container.
#
# Install (note: container /tmp is a tiny tmpfs — redirect TMPDIR to overlay disk or you'll
# hit "No space left on device"):
#   export TMPDIR=<scratchpad>/piptmp; export PIP_CACHE_DIR=$TMPDIR/cache
#   pip install --break-system-packages --no-cache-dir -r requirements-dev.txt
numpy
opencv-python-headless
pillow
